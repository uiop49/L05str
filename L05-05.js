/*function isDelResult(s1, s2) {
	res = '';
	for (var i = 0; i < s1.length; i++) {
		for (var j = 0; j < s2.length; j++) {
			if (s1[i] == s2[j]) {
				res += s2[j];
			}
		}
	}
	if (res.lenght >= s2.length) {
		return true;
	} else {
		return false;
	}

module.exports = isDelResult;*/